package com.example.ibmhackathon.domain;

import jakarta.persistence.Entity;

@Entity
public class StoreType extends Store {
    // TODO - Fill out info for big stores (Asda, Tesco etc) Maybe not if using API business?
}
